# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# Quantum computing (optional - will gracefully fallback if not available)
# dwave-ocean-sdk>=6.0.0
# dimod>=0.12.0

# Async and concurrency
asyncio-throttle>=1.0.2

# Configuration and logging
pyyaml>=6.0
python-dotenv>=0.19.0

# Performance and monitoring
psutil>=5.8.0
prometheus-client>=0.14.0

# API and web framework (for future REST API)
fastapi>=0.85.0
uvicorn[standard]>=0.18.0

# Development and testing dependencies (for development installs)
pytest>=7.0.0
pytest-asyncio>=0.19.0
pytest-cov>=3.0.0
black>=22.0.0
isort>=5.10.0
mypy>=0.971

# Optional: Real hardware integration
# pymodbus>=3.0.0  # For Modbus BMS integration
# opcua>=0.98.13   # For OPC-UA integration